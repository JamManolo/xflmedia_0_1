<h1> <%= @title %> </h1>

<%= form_for(@roster) do |f| %>
  <%= render 'rosters/fields', :f => f %>
  <div><%= f.hidden_field :group_id %></div> 
  <div class="actions">
    <%= f.submit "Create a roster" %>
  </div>
<% end %>
